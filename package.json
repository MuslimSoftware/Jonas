{
  "name": "jonas-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Monorepo for jonas",
  "scripts": {
    "mobile": "pnpm --filter mobile",
    "web": "pnpm --filter web",
    "backend": "pnpm --filter backend",
    "shared": "pnpm --filter @jonas/shared"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "packageManager": "pnpm@8.0.0",
  "pnpm": {
    "overrides": {
      "react-native": "0.76.9",
      "prettier": "^3.0.0"
    }
  }
} 